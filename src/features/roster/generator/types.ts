// Re-export from main roster types for backward compatibility
export type {
  BrigadeType,
  ShiftType,
  Employee,
  BrigadeRow,
  SupportServiceName,
  SupportPosition,
  SupportService,
  RosterData,
  RosterWarning,
  RosterGeneratorOptions,
} from '../types'
